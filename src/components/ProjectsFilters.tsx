import * as React from 'react';

export class ProjectsFilters extends React.PureComponent {
    public render(): React.ReactNode {
        return (
            <div>Project Filters</div>
        );
    }
}