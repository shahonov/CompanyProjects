import * as React from 'react';

export class CompaniesFilters extends React.PureComponent {
    public render(): React.ReactNode {
        return (
            <div>Company Filters</div>
        );
    }
}