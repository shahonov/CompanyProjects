import * as React from 'react';

export class EmployeesFilters extends React.PureComponent {
    public render(): React.ReactNode {
        return (
            <div>Employee Filters</div>
        );
    }
}